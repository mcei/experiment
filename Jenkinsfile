node {
    stage("Stage 1") {
        echo 'Hello World'
		echo "The status is ${currentBuild.currentResult}"
		sh 'echo this is shell'
        }
    }
    stage("Stage 2") {
        echo "We are at stage 2!"
    }
}